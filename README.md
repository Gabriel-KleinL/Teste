# Rotas ES — Deploy estático (GitHub Pages)

Este branch (`gh-pages`) contém apenas o **frontend estático** da aplicação
(mirror de `frontend/` do branch principal), publicado via GitHub Pages
para visualização direta no navegador.

O código-fonte completo do projeto (backend Python, algoritmos de TSP/VRP e
relatório acadêmico) está no branch
[`claude/delivery-routes-optimization-es-v5tl8s`](../../tree/claude/delivery-routes-optimization-es-v5tl8s).
